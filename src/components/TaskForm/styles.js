const styles = (theme) => ({
  form: {
    width: '100%',
    padding: theme.spacing(2, 4, 3),
  },

  textField: {
    marginBottom: '12px',
    padding: '8px 0',
    width: '100%',
    fontSize: '1.5rem',
  },

  dialogControls: {
    display: 'flex',
    justifyContent: 'flex-end',
    marginRight: '24px',
    padding: '12px 0',
  },
  button: {
    marginLeft: '12px',
  },
});

export default styles;
